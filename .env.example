# Chave da API Google Gemini (obrigatória para o chatbot em produção)
# Configure também em: Cloudflare Dashboard → Workers & Pages → Settings → Environment variables
GEMINI_API_KEY=your_gemini_api_key_here
